varnishtest "Test hashids vmod"

server s1 {
       rxreq
       txresp
} -start

varnish v1 -vcl+backend {
	import hashids from "${vmod_topbuild}/src/.libs/libvmod_hashids.so";

	sub vcl_deliver {
		set resp.http.hello = hashids.hello("World");
	}
} -start

client c1 {
	txreq -url "/"
	rxresp
	expect resp.http.hello == "Hello, World"
}

client c1 -run
